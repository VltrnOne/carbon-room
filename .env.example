# ============================================================================
# Carbon Room Environment Configuration
# ============================================================================
# Copy this file to .env and configure for your environment
# cp .env.example .env
# ============================================================================

# ----------------------------------------------------------------------------
# Environment Settings
# ----------------------------------------------------------------------------
# Options: development, staging, production
ENV=development

# Enable debug mode (set to false in production)
DEBUG=true

# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ----------------------------------------------------------------------------
# Database Configuration
# ----------------------------------------------------------------------------
# SQLite (development) - no additional setup required
DATABASE_URL=sqlite:///./carbon_room.db

# PostgreSQL (production) - uncomment for Render/production
# DATABASE_URL=postgresql://user:password@localhost:5432/carbon_room

# PostgreSQL Connection Pool (for production)
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10
DB_POOL_TIMEOUT=30

# ----------------------------------------------------------------------------
# API Settings
# ----------------------------------------------------------------------------
# Server host and port
API_HOST=0.0.0.0
API_PORT=8003

# API Key for authentication (generate a secure random key)
API_KEY=your-secure-api-key-here

# Secret key for JWT and session encryption (generate a secure random key)
# python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=carbon-room-dev-secret-change-in-production

# CORS allowed origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:8003

# ----------------------------------------------------------------------------
# GitHub Integration (for backups and CI/CD)
# ----------------------------------------------------------------------------
# Personal Access Token with repo permissions
# https://github.com/settings/tokens
GITHUB_TOKEN=

# Repository in format "owner/repo"
GITHUB_REPO=

# Branch for backups (default: main)
GITHUB_BRANCH=main

# Path in repo for backup files
GITHUB_BACKUP_PATH=backups

# ----------------------------------------------------------------------------
# Render.com Integration (for deployment)
# ----------------------------------------------------------------------------
# Render API Key
# https://dashboard.render.com/u/settings/api-keys
RENDER_API_KEY=

# Service ID from Render dashboard
RENDER_SERVICE_ID=

# Deploy hook URL (simple deployment trigger)
# Found in Service Settings > Deploy Hook
RENDER_DEPLOY_HOOK=

# ----------------------------------------------------------------------------
# SiteGround Integration (existing hosting)
# ----------------------------------------------------------------------------
# SSH host for SiteGround server
SITEGROUND_HOST=

# SSH username
SITEGROUND_USER=

# Path to SSH private key
SITEGROUND_KEY_PATH=

# ----------------------------------------------------------------------------
# Lightning Network Integration (payments/fast data)
# ----------------------------------------------------------------------------
# Lightning node REST API URL
LIGHTNING_NODE_URL=

# Macaroon for authentication (hex encoded)
LIGHTNING_MACAROON=

# Path to TLS certificate
LIGHTNING_TLS_CERT=

# ----------------------------------------------------------------------------
# Blockchain/Hash Settings
# ----------------------------------------------------------------------------
# Hash algorithm for content hashing
HASH_ALGORITHM=sha256

# Watermark prefix for Carbon[6] watermarks
WATERMARK_PREFIX=C6

# ----------------------------------------------------------------------------
# File Storage Settings
# ----------------------------------------------------------------------------
# Directory for file uploads
UPLOAD_DIR=./uploads

# Directory for generated certificates
CERTIFICATE_DIR=./certificates

# Maximum upload size in MB
MAX_UPLOAD_SIZE_MB=50

# ----------------------------------------------------------------------------
# Existing Keys (from original .env)
# ----------------------------------------------------------------------------
# Encryption key for vault files
KEY=

# API token for external services
API=
